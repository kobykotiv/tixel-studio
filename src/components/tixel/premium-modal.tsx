
"use client";

// This component is no longer used but is kept to avoid breaking imports if referenced elsewhere.
// The new ad-based model makes all features free.
export function PremiumModal() {
  return null;
}
